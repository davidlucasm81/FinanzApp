<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/gray_300"
    app:cardBackgroundColor="?attr/colorSurface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Debtor (From) -->
        <FrameLayout
            android:id="@+id/fl_avatar_from"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/iv_avatar_from"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/shape_circle"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/tv_initials_from"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textColor="@android:color/white"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="JD" />
        </FrameLayout>

        <!-- Connection & Amount -->
        <LinearLayout
            android:id="@+id/layout_middle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="@+id/fl_avatar_from"
            app:layout_constraintEnd_toStartOf="@+id/fl_avatar_to"
            app:layout_constraintStart_toEndOf="@+id/fl_avatar_from"
            app:layout_constraintTop_toTopOf="@+id/fl_avatar_from">

            <TextView
                android:id="@+id/tv_amount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="?attr/colorPrimary"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="23,50 €" />

            <ImageView
                android:id="@+id/iv_arrow"
                android:layout_width="32dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_arrow_forward"
                android:contentDescription="@null"
                app:tint="?attr/colorPrimary" />
        </LinearLayout>

        <!-- Creditor (To) -->
        <FrameLayout
            android:id="@+id/fl_avatar_to"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/iv_avatar_to"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/shape_circle"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/tv_initials_to"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textColor="@android:color/white"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="PM" />
        </FrameLayout>

        <!-- Action -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_settle"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:paddingHorizontal="16dp"
            android:text="@string/btn_settle"
            android:textAllCaps="false"
            android:textSize="13sp"
            app:cornerRadius="8dp"
            app:icon="@drawable/ic_check"
            app:iconPadding="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/fl_avatar_from" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>